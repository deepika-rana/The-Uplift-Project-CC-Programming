 #include <stdio.h>
int main()
{
    int i,j,r;
    int k=1;
    printf("Enter the row");
    scanf("%d",&r);
    for(i=0;i<r;++i)
    {
        for(j=0;j<=i;++j)
        {
            printf("%d",k);
            ++k;
            
        }
        printf("\n");
    }

    return 0;
}
